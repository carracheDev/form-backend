NEXT_PUBLIC_API_URL=your_backend_url_here
```

---

## **Important : `.gitignore`**

Vérifiez que `.env.local` est bien dans votre `.gitignore` pour ne pas exposer vos secrets sur GitHub :
```
.env.local